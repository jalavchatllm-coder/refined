import React from 'react';

// This component is not yet used, but providing a valid structure
// to prevent build errors from an empty file.
const ScoreCube: React.FC = () => {
  return null;
};

export default ScoreCube;